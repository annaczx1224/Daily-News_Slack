# Copy to .env for local runs:  cp .env.example .env  (then fill in the value)
# For the scheduled GitHub Action, set this as a repository *Secret* instead.
# This is the ONLY secret you need. No API keys, nothing to purchase.

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
